class RegionPolygon {

	constructor(...args){
		if (args.length < 3){
			// raises an error
		}
		// init polygon given set of at least two points
	}
	add_vertex(point){
		// if point is inside the polygon return false else true and add the point
		if (this.contais(point)){
			return false
		} else {

		}
	}
	extend(...args){

	}
	remove_vertex(point){

	}
	contains(point){

	}
}